apiVersion: eksctl.io/v1alpha5
kind: ClusterConfig

metadata:
  name: ekscluster_nm
  region: us-east-1

managedNodeGroups:
  - name: ng-1
    instanceType: t2.micro
    desiredCapacity: 2
  - name: spot-ng-2
    instanceType: ["t2.micro" , "t3.micro"]
    spot: true
    desiredCapacity: 2